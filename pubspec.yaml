name: ios_keyboard_action
description: This package adds an action button above the ios keyboard.
version: 0.0.3
homepage: https://github.com/warlesoncandido/IOSKeyBoardAction

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
